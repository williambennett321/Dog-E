<%- include('./partials/html-head') %>
<%- include('./partials/nav') %>

<main>
  <h1> <%= user ? user.profile.name : "Sign-in above!" %></h1>
  <% if (user?.profile.avatar) { %>
    <div>
      <img 
        src="<%= user.profile.avatar %>" 
        alt="<%= user.profile.name %> avatar"
      >
    </div>
  <% } %>
</main>

<%- include('./partials/footer') %>
